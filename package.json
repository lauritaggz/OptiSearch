{
  "name": "optisearch-prototipo-rt001",
  "version": "0.2.0",
  "private": true,
  "description": "Prototipo académico HU0 - OptiSearch - Mitigación RT-001",
  "scripts": {
    "test": "jest --verbose --runInBand",
    "test:categorizer": "jest tests/categorizer.test.js --verbose --runInBand",
    "test:dom": "jest tests/domAdapter.test.js --verbose --runInBand",
    "test:spike": "jest src/spike/contentAnalyzer/contentFetcher.test.js --verbose --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/tests/**/*.test.js",
      "**/src/**/*.test.js"
    ]
  }
}
